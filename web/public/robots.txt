# Robots policy for pergam.dev
# The landing page is crawlable; individual /s/<token> pergams are served
# by the viewer (view.html), which emits <meta name="robots" content="noindex">
# to keep user-submitted shares out of search indexes.

User-agent: *
Allow: /
